# ==============================================================
# requirements.txt
# Proyecto: RAG con búsqueda híbrida para normativa ambiental peruana
# Curso: Tópicos Avanzados de Base de Datos
# ==============================================================
# NOTAS DE ENTORNO:
#   - NB01, NB02, NB03 → ejecución local (VS Code, Python 3.11+)
#   - NB04, NB05        → Google Colab (GPU requerida para embeddings)
#   - Las versiones están fijadas a las que se verificaron funcionales.
#   - En Colab, instalar con la celda de instalación del notebook
#     (no usar pip install -r requirements.txt directamente en Colab).
# ==============================================================


# --------------------------------------------------------------
# 1. PROCESAMIENTO DE DOCUMENTOS
# Usados en NB02 (extracción de texto desde PDFs oficiales)
# --------------------------------------------------------------
pymupdf==1.24.9                 # Extracción de texto desde PDFs (importado como fitz)
pdfplumber==0.11.4              # Respaldo para PDFs con tablas complejas


# --------------------------------------------------------------
# 2. DATOS Y ANÁLISIS
# Usados en NB01–NB05 (manipulación de corpus, chunks y métricas)
# --------------------------------------------------------------
pandas==2.2.2
numpy==1.26.4


# --------------------------------------------------------------
# 3. EMBEDDINGS Y MODELOS
# Usados en NB04 (generación de vectores con BAAI/bge-m3)
# IMPORTANTE: en Colab se requieren versiones fijas por
# incompatibilidades entre torch, transformers y torchvision.
# --------------------------------------------------------------
torch==2.4.0                    # Instalación con: pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121
torchvision==0.19.0             # Instalación con: pip install torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
transformers==4.44.2            # Versión compatible con torch 2.4.0 y FlagEmbedding 1.2.11
FlagEmbedding==1.2.11           # Modelo BAAI/bge-m3 (multilingüe, orientado a recuperación)
sentence-transformers==3.0.1    # Alternativa si FlagEmbedding no está disponible


# --------------------------------------------------------------
# 4. BASE DE DATOS VECTORIAL
# Usados en NB04 (indexación) y NB05 (búsqueda semántica)
# NOTA: client.search() fue deprecado; se usa client.query_points()
# --------------------------------------------------------------
qdrant-client==1.10.1


# --------------------------------------------------------------
# 5. BÚSQUEDA LEXICAL
# Usados en NB05 (búsqueda BM25 sobre el corpus de chunks)
# --------------------------------------------------------------
rank-bm25==0.2.2


# --------------------------------------------------------------
# 6. GENERACIÓN DE RESPUESTAS (LLM)
# Usado en NB05 para generar respuestas sustentadas en fragmentos.
# NOTA: Gemini API fue reemplazado por Groq por restricción regional
# (Perú). Groq ofrece acceso gratuito a LLaMA 3.1.
# Modelo usado: llama-3.1-8b-instant
# --------------------------------------------------------------
groq==0.9.0


# --------------------------------------------------------------
# 7. EVALUACIÓN
# Usados en NB05 (métricas de recuperación y calidad de respuesta)
# --------------------------------------------------------------
scikit-learn==1.5.1
tqdm==4.66.4


# --------------------------------------------------------------
# 8. INTERFAZ DEMOSTRATIVA
# Usado en app/demo_streamlit.py
# --------------------------------------------------------------
streamlit==1.37.0


# --------------------------------------------------------------
# 9. UTILIDADES GENERALES
# --------------------------------------------------------------
python-dotenv==1.0.1            # Carga de variables de entorno (.env) para API keys
ipykernel==6.29.5               # Soporte para ejecución en Jupyter/VS Code
ipywidgets==8.1.3               # Widgets interactivos en notebooks (tqdm visual)


# --------------------------------------------------------------
# 10. EVALUACIÓN AVANZADA (OPCIONAL)
# Ragas puede usarse como apoyo para evaluar calidad RAG.
# No es obligatorio; la evaluación principal es propia (NB05).
# --------------------------------------------------------------
# ragas==0.1.21


# ==============================================================
# INSTALACIÓN LOCAL (NB01–NB03)
# ==============================================================
#   pip install -r requirements.txt
#
# INSTALACIÓN EN COLAB (NB04–NB05)
# Usar la celda de instalación incluida en cada notebook:
#
#   !pip install -q --upgrade pip
#   !pip uninstall -y transformers FlagEmbedding torchvision torchaudio torchcodec
#   !pip install -q torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
#   !pip install -q transformers==4.44.2
#   !pip install -q FlagEmbedding==1.2.11
#   !pip install -q rank-bm25 groq qdrant-client pandas numpy tqdm scikit-learn
#
# API KEYS necesarias (guardar en .env o Colab Secrets):
#   GROQ_API_KEY   → https://console.groq.com  (gratuito)
# ==============================================================
